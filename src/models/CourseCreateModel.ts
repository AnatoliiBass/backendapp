export type CourseCreateModel = {
    /**
     * The course name if you created course.
     */
    name: string;
    /**
     * The course author if you created course.
     */
    author_first_name: string;
    author_last_name: string;
}